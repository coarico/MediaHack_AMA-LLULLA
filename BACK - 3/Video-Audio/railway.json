{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "if [ -d 'BACK - 3/Video-Audio/app' ]; then cd 'BACK - 3/Video-Audio'; fi; uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/api/v1/health",
    "healthcheckTimeout": 600,
    "restartPolicyType": "ALWAYS"
  }
}
